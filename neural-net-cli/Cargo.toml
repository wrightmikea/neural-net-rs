[package]
name = "neural-net-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
neural-network = { path = "../neural-network" }
anyhow = "1"
chrono = "0.4"
indicatif = "0.17"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
